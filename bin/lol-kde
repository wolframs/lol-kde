#!/usr/bin/env python3
"""Entry point that works from a git checkout with no install step."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from lolkde.cli import main

if __name__ == "__main__":
    sys.exit(main())
